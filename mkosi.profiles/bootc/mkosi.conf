# No extra [Content] Packages= needed here -- bootc's own .deb declares its
# apt dependencies (dracut, ostree, libostree-dev) and apt resolves them
# automatically when mkosi.prepare.chroot installs it below. This is a real
# simplification versus the Arch version of this profile, not something
# skipped: Arch has no bootc/bootupd package at all, so that version had to
# build both from source.

# Build-time-only dependencies needed to compile bootc + bootupd from
# source, since Arch doesn't package either yet. Mirrors the
# `bootc-builder` stage in the old Containerfile.arch.
[Content]
Packages=
    make
    git
    rust
    go
    go-md2man
    ostree
    glibc
    pkgconf

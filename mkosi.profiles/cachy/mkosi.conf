# EXPERIMENTAL / opt-in, not part of the default build. Mirrors
# build_files/cachy.sh, which existed in the old Containerfile.arch repo
# but as far as I could tell was never actually invoked from any build
# stage there either -- so this is unverified in both the old and new
# build system. Enable with the `cachy` mkosi profile (ENABLE_CACHYOS=1 in
# the Justfile) if you want to try it, but test carefully.
[Content]
Packages=
    linux-cachyos
    linux-cachyos-headers
    tpm2-tss
